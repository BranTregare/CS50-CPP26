#include "include/helpers.hxx"
/*
// Convert image to grayscale
// void grayscale(int height, int width, RGBTRIPLE image[height][width]) { return; }

auto grayscale(auto Image) -> void
{
  for (auto [RowPos, ColPos] : mdindices(Image)) {}
  return;
}

// Convert image to sepia
auto sepia(auto Image) -> void
{
  for (auto [RowPos, ColPos] : mdindices(Image)) {}
  return;
}

// Reflect image horizontally
auto reflect(auto Image) -> void
{
  for (auto [RowPos, ColPos] : mdindices(Image)) {}
  return;
}

// Blur image
auto blur(auto Image) -> void
{
  for (auto [RowPos, ColPos] : mdindices(Image)) {}
  return;
}
*/